@*@model MrGroom_KY_SL.Models.Booking*@
@model MrGroom_KY_SL.Web.Models.BookingCreateViewModel

@*<p style="color:red;font-weight:bold">
    DEBUG EventDate: @Model.EventDate
</p>*@

@using MrGroom_KY_SL.Web.Models
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Create Booking";
}

<div class="booking-edit-wrapper py-5">
    <div class="booking-edit-card p-5 shadow-lg">
        <div class="text-center mb-5">
            <h2 class="fw-bold text-gradient">
                <i class="bi bi-plus-circle me-2"></i> Create Booking
            </h2>
            <p class="text-muted">Add a new booking with elegance and flair ✨</p>
        </div>

        @*@using (Html.BeginForm("Create", "AdminBookings", FormMethod.Post, new { id = "bookingForm" }))*@
        @*@using (Html.BeginForm(Model.BookingId > 0 ? "Edit" : "Create", "AdminBookings", FormMethod.Post, new { id = "bookingForm" }))*@
        @using (Html.BeginForm("CreateAjax", "AdminBookings", FormMethod.Post, new { id = "bookingForm" }))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(m => m.BookingId)

            <!-- Customer -->
            <div class="form-group mb-4">
                <label class="form-label fw-semibold">Customer</label>
                <div class="input-group">
                    <span class="input-group-text icon-gradient"><i class="bi bi-person"></i></span>
                    @*@Html.DropDownListFor(m => m.CustomerId, (SelectList)ViewBag.CustomerId, "- Select Customer -", new { @class = "form-control" })*@
                    @Html.DropDownListFor(m => m.CustomerId, (SelectList)ViewBag.Customers, "- Select Customer -", new { @class = "form-control" })
                </div>
                @Html.ValidationMessageFor(m => m.CustomerId, "", new { @class = "text-danger small" })
            </div>

            <!-- Package -->
            <div class="form-group mb-4">
                <label class="form-label fw-semibold">Package</label>
                <div class="input-group">
                    <span class="input-group-text icon-gradient"><i class="bi bi-box-seam"></i></span>
                    @*@Html.DropDownListFor(m => m.PackageId, (SelectList)ViewBag.PackageId, "- Select Package -", new { @class = "form-control" })*@
                    @Html.DropDownListFor(m => m.PackageId, (SelectList)ViewBag.Packages, "- Select Package -", new { @class = "form-control" })
                </div>
                @Html.ValidationMessageFor(m => m.PackageId, "", new { @class = "text-danger small" })
            </div>

            <div id="packageIncludes"
                 class="border rounded-3 p-3 mt-2 mb-4 bg-light d-none">


                <div class="d-flex align-items-center mb-2">
                    <i class="bi bi-check2-circle text-success me-2"></i>
                    <small class="fw-semibold text-muted">Package Includes</small>
                </div>

                <div id="packageIncludesList"></div>
            </div>


            <!-- Event Date -->
            <div class="form-group mb-4">
                <label class="form-label fw-semibold">Event Date</label>
                <div class="input-group">
                    <span class="input-group-text icon-gradient"><i class="bi bi-calendar3"></i></span>
                    @*@Html.TextBoxFor(m => m.EventDate, new { @class = "form-control", type = "date" })*@
                    @Html.TextBoxFor(m => m.EventDate, "{0:yyyy-MM-dd}", new { @class = "form-control", type = "date" })

                </div>
                @Html.ValidationMessageFor(m => m.EventDate, "", new { @class = "text-danger small" })
            </div>

            <!-- Event Location -->
            <div class="form-group mb-4">
                <label class="form-label fw-semibold">Location</label>
                <div class="input-group">
                    <span class="input-group-text icon-gradient">
                        <i class="bi bi-geo-alt"></i>
                    </span>
                    @Html.TextBoxFor(m => m.Location, new { @class = "form-control", placeholder = "Enter location" })
                </div>
                @Html.ValidationMessageFor(m => m.Location, "", new { @class = "text-danger small" })
            </div>

            <!-- Assign Staff -->
            <div class="form-group mb-4">
                <label class="form-label fw-semibold">Assign Staff (optional)</label>
                @*@Html.ListBoxFor(m => m.SelectedStaffIds, (MultiSelectList)ViewBag.SelectedStaffIds, new { @class = "form-control", size = 6 })*@
                @Html.ListBoxFor(m => m.SelectedStaffIds, (MultiSelectList)ViewBag.StaffList, new { @class = "form-control", size = 6 })
                @Html.ValidationMessageFor(m => m.SelectedStaffIds, "", new { @class = "text-danger small" })
            </div>

            <!-- Toggle Button -->
            <div class="mb-4 text-center">
                <button type="button"
                        id="btnToggleAddons"
                        class="btn btn-glow px-4 py-2">
                    <i class="bi bi-plus-circle me-2"></i>
                    Add Extras (Event Types & Items)
                </button>
            </div>

            <!-- Addons section-->
            <div id="addonsSection" style="display:none;">

                <div class="card shadow-sm border-0 mt-4">
                    <div class="card-body">

                        <h5 class="fw-bold mb-3">
                            <i class="bi bi-stars text-primary me-2"></i>
                            Addons
                        </h5>
                        <p class="text-muted small mb-4">
                            Select event types (one-time) and optional add-on items (per unit) to enhance the booking.
                        </p>

                        @{
                            var index = 0;
                        }

                        <div class="list-group list-group-flush">

                            <!-- Event Types-->
                            @if (ViewBag.EventTypes != null && ((IEnumerable<MrGroom_KY_SL.Models.EventType>)ViewBag.EventTypes).Any())
                            {
                                <div class="list-group-item bg-primary text-white fw-bold">
                                    Event Types
                                </div>

                                foreach (var et in (IEnumerable<MrGroom_KY_SL.Models.EventType>)ViewBag.EventTypes)
                                {
                                    var isChecked = Model.SelectedEventTypeIds?.Contains(et.EventTypeId) ?? false;
                                    <label class="list-group-item d-flex justify-content-between align-items-center">
                                        <div>
                                            <div class="fw-semibold">@et.Name</div>
                                            <small class="text-muted">One-time charge</small>
                                        </div>
                                        <div class="text-end">
                                            <div class="fw-bold">Rs @et.Price</div>
                                            <input type="checkbox"
                                                   class="form-check-input mt-1 event-type-checkbox"
                                                   name="SelectedEventTypeIds"
                                                   value="@et.EventTypeId"
                                                   data-eventtype-id="@et.EventTypeId"
                                                   @(isChecked ? "checked" : "") />
                                        </div>
                                    </label>
                                }

                            }

                            <!-- Addons-->
                            @if (ViewBag.PackageItems != null && ((IEnumerable<MrGroom_KY_SL.Models.PackageItem>)ViewBag.PackageItems).Any())
                            {
                                <div class="list-group-item bg-success text-white fw-bold mt-3">
                                    Package Items
                                </div>

                                foreach (var item in (IEnumerable<MrGroom_KY_SL.Models.PackageItem>)ViewBag.PackageItems)
                                {
                                    var existingAddon = Model.SelectedAddons?.FirstOrDefault(a => a.PackageItemId == item.PackageItemId);
                                    int quantity = existingAddon?.Quantity ?? 0;

                                    <div class="list-group-item d-flex justify-content-between align-items-center">
                                        <div>
                                            <div class="fw-semibold">@item.Name</div>
                                            <small class="text-muted">Per unit</small>
                                        </div>
                                        <div class="text-end d-flex align-items-center gap-2">
                                            <div class="fw-bold">Rs @item.Price</div>
                                            <input type="number"
                                                   class="form-control form-control-sm text-center addon-qty"
                                                   data-packageitem-id="@item.PackageItemId"
                                                   name="SelectedAddons[@index].Quantity"
                                                   value="@quantity"
                                                   min="0"
                                                   style="width:70px" />
                                            <input type="hidden"
                                                   name="SelectedAddons[@index].PackageItemId"
                                                   value="@item.PackageItemId" />
                                        </div>
                                    </div>
                                    index++;
                                }

                            }

                        </div>

                        @Html.ValidationMessageFor(m => m.SelectedEventTypeIds, "", new { @class = "text-danger small mt-3 d-block" })

                    </div>
                </div>
            </div>

            <!-- Buttons -->
            <div class="text-end mt-5 d-flex justify-content-end gap-3">
                @Html.ActionLink("Cancel", "Index", null, new { @class = "btn btn-glass px-4" })
                <button type="submit" id="btnCreateBooking" class="btn btn-glow px-4">
                    <i class="bi bi-save"></i> Create
                </button>
            </div>
        }
    </div>
</div>

<!--Payment Modal -->
<form id="paymentForm">
    <div class="modal fade" id="paymentModal" tabindex="-1" aria-labelledby="paymentModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content border-0 shadow-lg">
                <div class="modal-header bg-light">
                    <h5 class="modal-title fw-bold" id="paymentModalLabel">
                        <i class="bi bi-credit-card-2-front me-2 text-primary"></i>
                        Complete Payment
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>

                <div class="modal-body p-4">

                    <!-- Customer Name -->
                    <div class="card border-0 shadow-sm mb-4">
                        <div class="card-body d-flex align-items-center">
                            <i class="bi bi-person-circle fs-3 text-primary me-3"></i>
                            <div>
                                <div class="text-muted small">Customer</div>
                                <div class="fw-bold fs-5" id="paymentCustomer">—</div>
                            </div>
                        </div>
                    </div>

                    <!-- Booking Summary -->
                    <div class="card border-0 shadow-sm mb-3">
                        <div class="card-body py-3 px-3">
                            <input type="hidden" id="originalFullAmount" />
                            <h6 class="fw-semibold text-muted mb-2" style="font-size:0.85rem;">
                                <i class="bi bi-receipt me-1 text-info"></i>
                                Booking Summary
                            </h6>

                            <!-- Event Types -->
                            <div id="paymentEventTypesSection" class="mb-2">
                                <div class="fw-semibold text-secondary mb-1" style="font-size:0.8rem;">
                                    Event Types
                                </div>
                                <ul class="list-group list-group-flush small" id="paymentEventTypesList"></ul>
                            </div>

                            <!-- Add-ons -->
                            <div id="paymentAddonsSection">
                                <div class="fw-semibold text-secondary mb-1" style="font-size:0.8rem;">
                                    Add-ons
                                </div>
                                <ul class="list-group list-group-flush small" id="paymentAddonsList"></ul>
                            </div>

                        </div>
                    </div>


                    <!-- Package & Add-ons Amount Info -->
                    <div class="card border-0 shadow-sm mb-4">
                        <div class="card-body">

                            <h6 class="fw-bold text-uppercase text-muted mb-3">
                                <i class="bi bi-cash-stack me-2 text-success"></i>
                                Amount Summary
                            </h6>

                            <div class="d-flex justify-content-between mb-2">
                                <span class="text-muted">Package Amount</span>
                                <span class="fw-semibold">
                                    Rs <span id="packageAmount">0.00</span>
                                </span>
                            </div>

                            <div class="d-flex justify-content-between mb-2">
                                <span class="text-muted">Add-ons Amount</span>
                                <span class="fw-semibold">
                                    Rs <span id="paymentTotAddonsAmount">0.00</span>
                                </span>
                            </div>

                            <hr />

                            <div class="d-flex justify-content-between mb-2">
                                <span class="fw-semibold">Total Amount</span>
                                <span class="fw-bold fs-6">
                                    Rs <span id="paymentFullAmount">0.00</span>
                                </span>
                            </div>

                            <div class="d-flex justify-content-between mb-2 text-success">
                                <span>Previously Paid</span>
                                <span class="fw-semibold">
                                    Rs <span id="paymentPrevPaid">0.00</span>
                                </span>
                            </div>

                            <div class="d-flex justify-content-between align-items-center p-2 rounded bg-light">
                                <span class="fw-bold text-danger">Balance</span>
                                <span class="fw-bold fs-5 text-danger">
                                    Rs <span id="paymentBalance">0.00</span>
                                </span>
                            </div>

                        </div>
                    </div>

                    <!-- Discount Details -->
                    <div class="card border-0 shadow-sm mb-4 discount-highlight">
                        <div class="card-body">

                            <h6 class="fw-bold text-uppercase text-muted mb-3">
                                <i class="bi bi-percent me-2 text-warning"></i>
                                Discount Details
                            </h6>

                            <div class="row">

                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Discount Value (Rs)</label>

                                    <!--NAME IS REQUIRED -->
                                    <input type="number"
                                           name="DiscountValue"
                                           id="paymentDiscountValue"
                                           class="form-control"
                                           step="0.01"
                                           min="0" />
                                </div>

                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Discount (%)</label>

                                    <!--NAME IS REQUIRED -->
                                    <input type="number"
                                           name="DiscountPercentage"
                                           id="paymentDiscountPercentage"
                                           class="form-control"
                                           step="0.01"
                                           min="0" />
                                </div>

                            </div>
                        </div>
                    </div>


                    <!-- Payment Details -->
                    <div class="card border-0 shadow-sm">
                        <div class="card-body">

                            <h6 class="fw-bold text-uppercase text-muted mb-3">
                                <i class="bi bi-wallet2 me-2 text-primary"></i>
                                Payment Details
                            </h6>

                            <form id="paymentForm">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="BookingId" id="paymentBookingId" />

                                <div class="mb-3">
                                    <label class="form-label">Payment Amount</label>
                                    <input type="number"
                                           name="Amount"
                                           id="paymentAmount"
                                           class="form-control"
                                           step="0.01"
                                           required />
                                </div>

                                <div class="mb-3">
                                    <label class="form-label">Payment Method</label>
                                    <input type="text"
                                           name="PaymentMethod"
                                           class="form-control"
                                           placeholder="Cash, Card, Bank Transfer"
                                           required />
                                </div>

                                <div class="mb-3">
                                    <label class="form-label">Payment Type</label>
                                    <select name="PaymentType" class="form-select" required>
                                        <option value="">- Select Type -</option>
                                        <option>Advance</option>
                                        <option>Half</option>
                                        <option>Full</option>
                                    </select>
                                </div>

                                <div class="mb-3">
                                    <label class="form-label">Remarks</label>
                                    <textarea name="Remarks" class="form-control" rows="2"></textarea>
                                </div>
                            </form>

                        </div>
                    </div>

                </div>

                <div class="modal-footer bg-light">
                    <button class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button class="btn btn-primary px-4" id="btnSavePayment">
                        <i class="bi bi-save me-1"></i> Save Payment
                    </button>
                </div>
            </div>
        </div>
    </div>
</form>
    @section Scripts {
        <script>

            let autoPayment = true;

    // ============================
    // GLOBAL FUNCTIONS
    // ============================

    function formatCurrency(amount) {
        return Number(amount || 0).toLocaleString('en-US', {
            minimumFractionDigits: 2,
            maximumFractionDigits: 2
        });
    }

    function parseCurrency(text) {
        return parseFloat((text || "").replace(/[^0-9.-]+/g, '')) || 0;
            }

            let remaining = 0;
    //function updateBalanceAfterDiscount() {

    //    let fullAmount = parseFloat($('#originalFullAmount').val()) || 0;
    //    let prevPaid   = parseCurrency($('#paymentPrevPaid').text());
    //    let entered    = parseFloat($('#paymentAmount').val()) || 0;
    //    let discount   = parseFloat($('#paymentDiscountValue').val()) || 0;

    //    let discountedTotal = fullAmount - discount;

    //    if (discountedTotal < 0)
    //        discountedTotal = 0;

    //    //Total Amount auto updates here
    //    $('#paymentFullAmount').text(formatCurrency(discountedTotal));

    //    remaining = discountedTotal - prevPaid - entered;

    //    if (remaining < 0)
    //        remaining = 0;

    //    $('#paymentBalance').text(formatCurrency(remaining));
    //    $('#paymentAmount').val(remaining.toFixed(2));
    //}
            function updateAmounts() {

    let fullAmount = parseFloat($('#originalFullAmount').val()) || 0;
    let prevPaid   = parseCurrency($('#paymentPrevPaid').text());
    let discount   = parseFloat($('#paymentDiscountValue').val()) || 0;
    let entered    = parseFloat($('#paymentAmount').val()) || 0;

    // Prevent over-discount
    if (discount > fullAmount) {
        discount = fullAmount;
        $('#paymentDiscountValue').val(fullAmount.toFixed(2));
    }

    let newTotal = fullAmount - discount;

    if (newTotal < 0)
        newTotal = 0;

    $('#paymentFullAmount').text(formatCurrency(newTotal));

    let balance = newTotal - prevPaid - entered;

    if (balance < 0)
        balance = 0;

    $('#paymentBalance').text(formatCurrency(balance));
}

            $(document).on('input', '#paymentDiscountValue', function () {

                let fullAmount = parseCurrency($('#paymentFullAmount').text());
                let discountValue = parseFloat($(this).val()) || 0;

                if (discountValue > fullAmount) {
                    discountValue = fullAmount;
                    $(this).val(fullAmount);
                }

                let percent = (discountValue / fullAmount) * 100;
                $('#paymentDiscountPercentage').val(percent.toFixed(2));

                //updateAmounts();
            });

    function setButtonLoading(btn, loadingText) {
        btn.data('original-text', btn.html());
        btn.prop('disabled', true);
        btn.html(`<span class="spinner-border spinner-border-sm me-2"></span>${loadingText}`);
    }

    function resetButton(btn) {
        btn.prop('disabled', false);
        btn.html(btn.data('original-text'));
    }

    function loadPackageIncludes(packageId) {

        if (!packageId) {
            $('#packageIncludesList').empty();
            $('#packageIncludes').addClass('d-none');
            return;
        }

        $.get('/AdminBookings/GetPackageAddons', { packageId }, function (res) {

            if (!res.success) return;

            let html = '';

            if (res.eventTypes && res.eventTypes.length) {

                html += `
                <div class="package-section">
                    <div class="package-section-title">Event Types</div>
                    <div class="package-chips">`;

                res.eventTypes.forEach(e => {
                    html += `
                        <span class="package-chip">
                            ${e.name}
                            <span class="chip-badge included">Included</span>
                        </span>`;
                });

                html += `</div></div>`;
            }

            if (res.items && res.items.length) {

                const validItems = res.items.filter(i => i.quantity > 0);
                if (validItems.length) {

                    html += `
                    <div class="package-section mt-2">
                        <div class="package-section-title">Items</div>
                        <div class="package-chips">`;

                    validItems.forEach(i => {
                        html += `
                        <span class="package-chip">
                            ${i.name}
                            <span class="chip-badge qty">Qty ${i.quantity}</span>
                        </span>`;
                    });

                    html += `</div></div>`;
                }
            }

            $('#packageIncludesList').html(html);
            $('#packageIncludes').removeClass('d-none');
        });
    }

    function populatePaymentModal(res) {

        $('#paymentBookingId').val(res.bookingId);
        $('#paymentCustomer').text(res.customerName);
        //$('#paymentFullAmount').text(formatCurrency(res.fullAmount));
        $('#originalFullAmount').val(res.fullAmount);
        $('#paymentFullAmount').text(formatCurrency(res.fullAmount));
        $('#packageAmount').text(formatCurrency(res.packageAmount));
        $('#paymentAddonsAmount').text(formatCurrency(res.addonsAmount));
        $('#paymentPrevPaid').text(formatCurrency(res.prevPaid));
        $('#paymentTotAddonsAmount').text(formatCurrency(res.totalAddonsAmount));

        //const balance = res.fullAmount - res.prevPaid;
        //$('#paymentBalance').text(formatCurrency(balance));
        //$('#paymentAmount').val(balance.toFixed(2));
        //$('#paymentBalance').text(formatCurrency(remaining));
        //$('#paymentAmount').val(remaining.toFixed(2));
    //        const balance = res.fullAmount - res.discountVal;
    //$('#paymentBalance').text(formatCurrency(balance - res.prevPaid));
    //    $('#paymentAmount').val(balance - res.prevPaid);

        //const remaining = (res.fullAmount - res.discountVal) - res.prevPaid;
        //$('#paymentBalance').text(formatCurrency(remaining));
        //$('#paymentAmount').val(remaining);

    //        const balance = res.fullAmount - res.discountVal;
    //$('#paymentBalance').text(formatCurrency(balance));
    //$('#paymentAmount').val(balance);
        // ===============================
        // DISCOUNT SHOW ONLY IF EXISTS
        // ===============================
        if (res.discountVal > 0) {
            $('#paymentDiscountValue').val(res.discountVal.toFixed(2));
            $('#paymentDiscountPercentage').val(res.discountPercent);

            $('#paymentDiscountValue').prop('readonly', true);
            $('#paymentDiscountPercentage').prop('readonly', true);
        } else {
        $('#paymentDiscountValue').val('');
        $('#paymentDiscountPercentage').val('');
        }


        // Set initial payment = full balance (once only)
        let initialBalance = res.fullAmount - res.prevPaid;
        //$('#paymentAmount').val(initialBalance.toFixed(2));
        autoPayment = true;


        updateAmounts();
    }

    // ============================
    // DOM READY
    // ============================

    $(function () {

        // PAYMENT INPUT
        $(document).on('input', '#paymentAmount', function () {
            autoPayment = false;
            updateAmounts();
        });

        // DISCOUNT INPUT
        $(document).on('input', '#paymentDiscountValue', function () {

            let fullAmount = parseFloat($('#originalFullAmount').val()) || 0;
            let discountValue = parseFloat($(this).val()) || 0;

            if (discountValue > fullAmount) {
                discountValue = fullAmount;
                $(this).val(fullAmount.toFixed(2));
            }

            let newTotal = fullAmount - discountValue;

            // If payment was auto-filled, adjust it
            if (autoPayment) {
                $('#paymentAmount').val(newTotal.toFixed(2));
            }

            updateAmounts();
        });
        //$(document).on('input', '#paymentDiscountValue', function () {

        //    let fullAmount = parseFloat($('#originalFullAmount').val()) || 0;
        //    let discountValue = parseFloat($(this).val()) || 0;

        //    if (discountValue > fullAmount) {
        //        discountValue = fullAmount;
        //        $(this).val(fullAmount);
        //    }

        //    let discountPercent = (discountValue / fullAmount) * 100;
        //    $('#paymentDiscountPercentage').val(discountPercent.toFixed(2));

        //    updateAmounts();
        //});

        // PACKAGE DROPDOWN
        $(document).on('change', '#PackageId', function () {

            const packageId = $(this).val();

            if (!packageId) {
                $('#packageIncludesList').empty();
                $('#packageIncludes').addClass('d-none');
                return;
            }

            loadPackageIncludes(packageId);
        });

        // TOGGLE ADDONS
        $(document).on('click', '#btnToggleAddons', function () {

            const section = $('#addonsSection');
            section.slideToggle(200);

            if (section.is(':visible')) {
                $(this).html('<i class="bi bi-dash-circle"></i> Hide Items / Event Types');
            } else {
                $(this).html('<i class="bi bi-plus-circle"></i> Add Items / Event Types');
            }
        });

        // BOOKING FORM SUBMIT
        $(document).on('submit', '#bookingForm', function (e) {

            e.preventDefault();

            const btn = $('#btnCreateBooking');
            setButtonLoading(btn, 'Creating...');

            $.ajax({
                @*url: '@Url.Action("CreateAjax", "AdminBookings")',*@
                url: $('#bookingForm').attr('action'),
                type: 'POST',
                data: $(this).serialize(),
                success: function (res) {

                    if (!res.success) {
                        toastr.error(res.message);
                        resetButton(btn);
                        return;
                    }

                    if (res.unpaid) {

                        populatePaymentModal(res);

                        const modal = new bootstrap.Modal(
                            document.getElementById('paymentModal')
                        );
                        modal.show();

                        resetButton(btn);
                        return;
                    }

                    toastr.success('Booking completed');
                    window.location.href = '@Url.Action("Index", "AdminBookings")';
                },
                error: function () {
                    toastr.error('Error creating booking.');
                    resetButton(btn);
                }
            });
        });

        // SAVE PAYMENT
        $(document).on('click', '#btnSavePayment', function () {

            const btn = $(this);
            setButtonLoading(btn, 'Saving...');

            $.ajax({
                url: '@Url.Action("SavePayment", "AdminBookings")',
                type: 'POST',
                data: $('#paymentForm').serialize(),
                success: function (res) {

                    if (res.success) {

                        toastr.success("Payment saved!");
                        $('#paymentModal').modal('hide');

                        if (res.invoiceUrl) {
                            setTimeout(() => {
                                window.open(res.invoiceUrl, "_blank");
                            }, 800);
                        }

                        setTimeout(() => {
                            window.location.href = '@Url.Action("Index", "AdminBookings")';
                        }, 2000);

                    } else {
                        toastr.warning(res.message || "Unable to save payment.");
                        resetButton(btn);
                    }
                },
                error: function () {
                    toastr.error("Error saving payment.");
                    resetButton(btn);
                }
            });
        });

    });

        </script>
    }

    @section Styles {
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />
        <style>
            body {
                background: linear-gradient(120deg, #e0e7ff, #f5f3ff, #f0f9ff);
                background-size: 300% 300%;
                animation: bgShift 12s ease infinite;
                font-family: 'Poppins', 'Segoe UI', sans-serif;
                color: #1e293b;
            }

            @@keyframes bgShift {
                0% {
                    background-position: 0% 50%;
                }

                50% {
                    background-position: 100% 50%;
                }

                100% {
                    background-position: 0% 50%;
                }
            }

            .booking-edit-wrapper {
                display: flex;
                justify-content: center;
                align-items: flex-start;
            }

            .booking-edit-card {
                width: 100%;
                max-width: 750px;
                background: #ffffff;
                border-radius: 20px;
                position: relative;
                overflow: hidden;
                box-shadow: 0 8px 20px rgba(0,0,0,0.05), inset 2px 2px 6px rgba(255,255,255,0.6), inset -2px -2px 8px rgba(0,0,0,0.05);
                animation: fadeIn 0.8s ease;
            }

                .booking-edit-card::before {
                    content: "";
                    position: absolute;
                    top: 0;
                    left: 0;
                    width: 100%;
                    height: 6px;
                    background: linear-gradient(90deg, #4b6cb7, #43cea2, #4b6cb7);
                    animation: gradientFlow 6s linear infinite;
                }

            @@keyframes gradientFlow {
                0%, 100% {
                    background-position: 0% 50%;
                }

                50% {
                    background-position: 100% 50%;
                }
            }

            @@keyframes fadeIn {
                from {
                    opacity: 0;
                    transform: translateY(15px);
                }

                to {
                    opacity: 1;
                    transform: translateY(0);
                }
            }

            .text-gradient {
                background: linear-gradient(90deg, #185a9d, #43cea2);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
            }

            .form-label {
                color: #374151;
                font-weight: 600;
            }

            .input-group-text {
                background: #f8fafc;
                border: 1.5px solid #e2e8f0;
                border-radius: 10px 0 0 10px;
            }

            .icon-gradient i {
                background: linear-gradient(90deg, #43cea2, #185a9d);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
            }

            .form-control {
                background: #f8fafc;
                border: 1.5px solid #e2e8f0;
                border-radius: 0 10px 10px 0;
                color: #1e293b;
                transition: all 0.3s ease;
            }

                .form-control:focus {
                    border-color: #4b6cb7;
                    box-shadow: 0 0 0 3px rgba(75,108,183,0.25);
                }

            .btn-glow {
                background: linear-gradient(90deg, #43cea2, #185a9d);
                color: #fff;
                box-shadow: 0 4px 12px rgba(24,90,157,0.3);
            }

                .btn-glow:hover {
                    transform: translateY(-2px);
                    box-shadow: 0 8px 20px rgba(24,90,157,0.4);
                }

            .input-group, .form-group {
                background: #fafafa;
                border-radius: 8px;
                padding: 8px;
            }

            /*  Modern Validation */
            .text-danger.small {
                display: none;
            }

            .field-validation-error.text-danger.small {
                display: flex;
                align-items: center;
                gap: 6px;
                color: #d32f2f;
                font-size: 0.85rem;
                font-weight: 500;
                margin-top: 6px;
                background: #fdf2f2;
                border-radius: 6px;
                padding: 6px 10px;
            }

                .field-validation-error.text-danger.small::before {
                    content: "⚠️";
                    font-size: 0.9rem;
                    margin-right: 4px;
                }

            input.input-validation-error, select.input-validation-error, textarea.input-validation-error {
                border-color: #e53935 !important;
                background-color: #fff;
                color: #b91c1c;
                box-shadow: 0 0 0 3px rgba(229,57,53,0.15);
                transition: all 0.3s ease;
            }

            .saved-card {
                position: relative;
                gap: 15px;
            }

                .saved-card img {
                    max-height: 24px;
                }

                .saved-card button {
                    white-space: nowrap;
                }

            /*booking summary*/
            .booking-summary-pills {
                padding: 10px;
                background: #f8f9fa;
                border-radius: 10px;
                border: 1px solid #e4e4e4;
            }

            .pill-row {
                display: flex;
                justify-content: space-between;
                flex-wrap: wrap;
                gap: 10px;
            }

            .pill {
                background: #ffffff;
                border-radius: 30px;
                padding: 10px 15px;
                box-shadow: 0 2px 6px rgba(0,0,0,0.1);
                min-width: 120px;
                text-align: center;
            }

            .pill-label {
                display: block;
                font-size: 0.75rem;
                color: #6c757d;
                text-transform: uppercase;
                letter-spacing: 0.5px;
                margin-bottom: 3px;
            }

            .pill-value {
                font-size: 1rem;
                font-weight: 600;
                color: #212529;
            }

            .addon-card {
                background: #fff;
                border-radius: 12px;
                border: 1px solid #eee;
                cursor: pointer;
                transition: all 0.2s ease;
            }

                .addon-card:hover {
                    box-shadow: 0 8px 20px rgba(0,0,0,0.08);
                    transform: translateY(-2px);
                }

            .btn-glow {
                background: linear-gradient(135deg, #6f42c1, #0d6efd);
                color: #fff;
                border: none;
            }

                .btn-glow:hover {
                    opacity: 0.95;
                }


            /* Package Includes – FIXED layout */

            .package-section-title {
                font-size: 0.75rem;
                font-weight: 600;
                color: #374151;
                margin-bottom: 6px;
            }

            .package-chips {
                display: flex;
                flex-wrap: wrap;
                gap: 6px;
            }

            .package-chip {
                background: #ffffff;
                border: 1px solid #e5e7eb;
                border-radius: 999px;
                padding: 4px 10px;
                display: inline-flex;
                align-items: center;
                gap: 6px;
                font-size: 0.75rem;
                box-shadow: 0 1px 2px rgba(0,0,0,0.04);
                white-space: nowrap;
            }

            .chip-badge {
                font-size: 0.65rem;
                padding: 2px 6px;
                border-radius: 999px;
                font-weight: 600;
            }

                .chip-badge.included {
                    background: #dcfce7;
                    color: #166534;
                }

                .chip-badge.qty {
                    background: #e0f2fe;
                    color: #075985;
                }


            /* Discount styles*/
            .discount-highlight {
                background: linear-gradient(135deg, #fff7ed, #fffbeb);
                border-left: 5px solid #f59e0b;
                border-radius: 12px;
                transition: all 0.3s ease;
            }

                .discount-highlight:hover {
                    box-shadow: 0 10px 25px rgba(245, 158, 11, 0.25);
                    transform: translateY(-2px);
                }
        </style>
    }
